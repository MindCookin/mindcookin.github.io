var test = require('tape');
var contact = require('../Contact.react');

test('contact', function (t) {

  t.plan(1);
  // Contact should be unique
    // should be able to receive a name, an email and a phone
  t.end();
});
