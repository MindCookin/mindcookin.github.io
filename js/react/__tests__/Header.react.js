var test = require('tape');
var List = require('../List.react.js');

test('header', function (t) {

    t.plan(1);
  // Header should create unique instances
    // it should be able to receive a list of languages
    // it should be able to receive a title
    // it should be able to have animations?

  // Header should expose html
    // html should be as expected

    t.end();
});
